export interface Experience {
  company: string;
  title: string;
  duration: string;
  description?: string;
}

export interface Education {
  school: string;
  location: string;
  course: string;
  detail: string;
}